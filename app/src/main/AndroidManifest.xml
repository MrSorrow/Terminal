<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ping.terminal">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoTitleBar">
        <activity
            android:name=".LoadActivity"
            android:configChanges="orientation|keyboardHidden"
            android:theme="@style/LoadTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".ScanDeviceActivity"></activity>
        <activity android:name=".SendCharOrder"></activity>
        <activity
            android:name=".SendTextOrder"
            android:windowSoftInputMode="stateHidden|adjustResize"></activity>
        <activity android:name=".SendImageOrder"></activity>
        <activity android:name=".SendFigureOrder"></activity>
        <activity android:name=".SendWaveOrder"></activity>
        <activity android:name=".SendPictureOrder"></activity>
        <activity android:name=".IntroduceActivity"></activity>
    </application>

</manifest>